{{- if .Values.keycloak.enabled }}
apiVersion: v1.edp.epam.com/v1
kind: Keycloak
metadata:
  name: {{ include "curq.fullname" . }}
spec:
  secret: {{ .Values.keycloak.secret | quote }}
  url: {{ .Values.keycloak.endpoint | quote }}
{{- end }}
