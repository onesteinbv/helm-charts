apiVersion: v1
kind: Service
metadata:
  name: {{ include "odoo-upgrader.fullname" . }}
  labels:
    {{- include "odoo-upgrader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odoo-upgrader.selectorLabels" . | nindent 4 }}
